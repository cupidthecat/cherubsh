[package]
name = "cherubsh-test-harness"
version = "0.2.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
cherubsh-common = { path = "../common" }
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
