name: hardening

on:
  schedule:
    - cron: "23 4 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: hardening-${{ github.ref }}
  cancel-in-progress: true

jobs:
  generated-parity:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install --yes \
            autoconf \
            bison \
            build-essential \
            clang \
            curl \
            git \
            gpgv \
            libncurses-dev \
            patch \
            perl \
            python3 \
            texinfo \
            util-linux

      - name: Install the pinned Rust toolchain
        run: rustup toolchain install 1.93.1 --profile minimal

      - name: Prepare the Bash oracle
        run: |
          ./tools/fetch-upstream.sh
          oracle/build-bash-5.3.15.sh

      - name: Build CherubSH
        run: cargo build --locked -p cherubsh

      - name: Run generated Bash comparisons
        env:
          FUZZ_CASES: "500"
          FUZZ_ARTIFACT_DIR: target/hardening/fuzz
        run: ./tools/run-fuzz-smoke.sh

      - name: Run interactive PTY comparisons
        run: |
          python3 tools/pty-differential.py \
            --bash target/oracle/bash-5.3.15/bash \
            --cherub target/debug/cherubsh \
            --report-dir target/hardening/pty
          python3 tools/pty-stress.py \
            --bash target/oracle/bash-5.3.15/bash \
            --cherub target/debug/cherubsh \
            --rounds 20 \
            --report-dir target/hardening/pty-stress

      - name: Preserve hardening artifacts
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: hardening-artifacts
          path: target/hardening
          if-no-files-found: ignore

  large-bash-corpus:
    name: large-bash-corpus
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install --yes \
            autoconf \
            bison \
            bubblewrap \
            build-essential \
            curl \
            git \
            gpgv \
            libncurses-dev \
            patch \
            python3 \
            texinfo \
            util-linux

      - name: Install the pinned Rust toolchain
        run: rustup toolchain install 1.93.1 --profile minimal

      - name: Prepare the Bash oracle
        run: |
          ./tools/fetch-upstream.sh
          oracle/build-bash-5.3.15.sh

      - name: Build CherubSH
        run: |
          cargo build --locked -p cherubsh
          cargo build --locked --release -p cherubsh

      - name: Parse the pinned large Bash corpus
        run: |
          python3 tools/large-script-parity.py \
            --bash target/oracle/bash-5.3.15/bash \
            --cherub target/debug/cherubsh

      - name: Run the pinned program smoke matrix
        run: |
          python3 tools/program-smoke.py \
            --bash target/oracle/bash-5.3.15/bash \
            --cherub target/release/cherubsh \
            --timeout 30

      - name: Preserve large Bash corpus failures
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: large-bash-corpus-report
          path: |
            target/hardening/large-scripts
            target/hardening/program-smoke
          if-no-files-found: ignore

  address-sanitizer:
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Clang
        run: sudo apt-get update && sudo apt-get install --yes clang

      - name: Install the sanitizer toolchain
        run: rustup toolchain install nightly-2026-07-30 --profile minimal --component rust-src

      - name: Run the Rust and C bridge tests under AddressSanitizer
        env:
          ASAN_OPTIONS: detect_leaks=1:halt_on_error=1
          CC: clang
          CHERUBSH_C_SANITIZER: address
          RUSTFLAGS: -Zsanitizer=address
        run: |
          cargo +nightly-2026-07-30 test -Zbuild-std \
            --target x86_64-unknown-linux-gnu \
            --workspace \
            --locked
