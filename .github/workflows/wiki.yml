name: wiki

on:
  push:
    branches:
      - main
    paths:
      - 'wiki/**'
      - 'tools/check-wiki-source.sh'
      - 'tools/publish-wiki.sh'
      - 'tools/install-git-hooks.sh'
      - '.githooks/pre-commit'
      - '.github/workflows/wiki.yml'
  pull_request:
    paths:
      - 'wiki/**'
      - 'tools/check-wiki-source.sh'
      - 'tools/publish-wiki.sh'
      - 'tools/install-git-hooks.sh'
      - '.githooks/pre-commit'
      - '.github/workflows/wiki.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: wiki-publish
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Validate wiki source
        run: ./tools/check-wiki-source.sh

  publish:
    needs: validate
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6

      - name: Configure the wiki deploy key
        env:
          WIKI_DEPLOY_KEY: ${{ secrets.WIKI_DEPLOY_KEY }}
        run: |
          if [ -z "$WIKI_DEPLOY_KEY" ]; then
            echo 'WIKI_DEPLOY_KEY is not configured' >&2
            exit 1
          fi
          install -d -m 700 "$HOME/.ssh"
          printf '%s\n' "$WIKI_DEPLOY_KEY" | tr -d '\r' > "$HOME/.ssh/wiki_deploy_key"
          chmod 600 "$HOME/.ssh/wiki_deploy_key"
          if ! ssh-keygen -y -f "$HOME/.ssh/wiki_deploy_key" >/dev/null 2>&1; then
            echo 'WIKI_DEPLOY_KEY must contain an unencrypted OpenSSH private key' >&2
            exit 1
          fi
          ssh-keyscan -t ed25519 github.com >> "$HOME/.ssh/known_hosts"
          chmod 600 "$HOME/.ssh/known_hosts"
          printf '%s\n' "GIT_SSH_COMMAND=ssh -i $HOME/.ssh/wiki_deploy_key -o IdentitiesOnly=yes" >> "$GITHUB_ENV"

      - name: Publish source pages to GitHub Wiki
        env:
          WIKI_REPOSITORY: git@github.com:${{ github.repository }}.wiki.git
          WIKI_COMMIT_NAME: cherubsh wiki publisher
          WIKI_COMMIT_EMAIL: 41898282+github-actions[bot]@users.noreply.github.com
        run: ./tools/publish-wiki.sh wiki "$RUNNER_TEMP/cherubsh.wiki"
